pytest>=8.0.0
pytest-asyncio>=0.24.0
# Tracks the Home Assistant release the live instance runs. The suite mocks
# HTTP through homeassistant's own AiohttpClientMocker, which ships inside this
# package, so there is no separate mocking library to keep in step with aiohttp.
pytest-homeassistant-custom-component>=0.13.316
